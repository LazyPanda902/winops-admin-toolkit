# AI Daily Plan - 2026-06-07

## Provider

claude

## Status

ok

## Limited

False

## Task

Suggest one small real daily improvement for winops-admin-toolkit. Project angle: IT support automation. Prefer a safe change such as docs, tests, config example, parser improvement, or a small zero-dependency feature. Do not suggest fake progress.

## Output

The project is minimal — just a `health_check()` stub. Here's one real, small improvement:

---

**1. Summary**

Add a `WindowsEventLogParser` module to `src/event_log_parser.py`. Windows admins commonly export Event Viewer logs as CSV files. A small stdlib-only parser that reads those exports and filters by level (Error/Warning) or Event ID is a real, useful IT support automation primitive — and a natural first feature for this project.

**2. Proposed files to change**

- **New:** `src/event_log_parser.py` — reads a CSV-format Windows event log export, returns filtered records by level or event ID, using only `csv` from stdlib.
- **New:** `tests/test_event_log_parser.py` — tests using in-memory `StringIO` CSV data (no disk I/O, no external deps).

**3. Safety notes**

- Zero new dependencies (uses `csv` and `io` from stdlib only).
- No file system side effects in tests.
- No secrets, no external calls, no platform-specific syscalls.
- Does not claim to replace or copy any existing tool.

**4. Commit message**

```
feat(parser): add Windows event log CSV parser with level/id filtering
```

---

Want me to implement it now?

## Safety

This plan is inspiration only. The automation applies controlled changes and must still pass checks before commit.
